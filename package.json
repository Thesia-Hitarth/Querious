{
  "name": "stack-overflow-clone-root",
  "version": "1.0.0",
  "description": "Root workspace setup for Stack Overflow Clone",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run start --prefix client\"",
    "build": "npm run build --prefix client",
    "clear-db": "node server/clearDb.js",
    "server": "npm run dev --prefix server",
    "client": "npm run start --prefix client",
    "test:server": "cross-env NODE_OPTIONS=--experimental-vm-modules npx --prefix server jest --testPathPatterns=server --runInBand --forceExit",
    "test:client": "cross-env CI=true npm run test --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^10.1.0"
  }
}
